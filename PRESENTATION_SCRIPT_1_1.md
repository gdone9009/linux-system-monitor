# 🎤 [구조화 발표 스크립트] 1-1. 기본 보안 및 네트워크 설정 개조식 시연 가이드

> **목적**: 1-1 미션 평가 시 다른 사람이나 평가자에게 말하듯이 자신감 있게 설명할 수 있도록 **구조화된 목차 기반 개조식 대본**으로 작성된 발표 가이드입니다.

---

## 📌 1. 도입 및 서론 (Intro)

* **1.1 인사 및 주제 소개**
  * 안녕하세요. 이번 **[1-1 기본 보안 및 네트워크 설정]** 미션의 발표 및 시연을 맡은 발표자입니다.
  * 본 시연에서는 서버 운영의 1대 수칙인 **SSH 접속 보안 강화(Hardening)** 및 **UFW 방화벽 패킷 통제**를 직접 시연하고 설명하겠습니다.

* **1.2 문제의식 제기 (왜 보안 요새화가 필요한가?)**
  * 인터넷에 연결된 서버의 기본 22번 포트는 24시간 내내 무차별 대입 공격(Brute-force)의 타깃이 됩니다.
  * 따라서 **비표준 포트 전환**, **Root 접속 원천 차단**, **최소 포트 방화벽 개방**이라는 3중 방어선 구축이 필수적입니다.

---

## 🛡️ 2. SSH 보안 요새화 시연 및 해설 (SSH Hardening)

* **2.1 핵심 설정 내용 설명**
  * **SSH 포트 20022 변경**: 자동 공격 봇의 22번 포트 스캔을 무력화하여 99%의 공격 시도 차단.
  * **Root 원격 로그인 차단 (`PermitRootLogin no`)**: 해커가 최고의 권한자인 Root로 직접 접속하는 통로를 봉쇄.

* **2.2 터미널 시연 진행**
  * **실행 명령어**:
    ```bash
    grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
    ```
  * **시연 설명 멘트**:
    * "보시는 화면과 같이 `sshd_config` 파일에 `Port 20022`와 `PermitRootLogin no`가 적용되어 있습니다."
    * "이로써 외부에서는 절대 22번 포트나 Root 계정으로 직접 진입할 수 없습니다."

* **2.3 소켓 LISTEN 상태 검증**
  * **실행 명령어**:
    ```bash
    sudo ss -tulnp | grep 20022
    ```
  * **시연 설명 멘트**:
    * "`ss -tulnp` 명령어로 확인한 결과, SSH 데몬이 20022번 TCP 포트에서 정상적으로 LISTEN 상태로 수신 대기 중입니다."

---

## 🔥 3. UFW 방화벽 화이트리스트 구축 시연 및 해설 (UFW Firewall)

* **3.1 방화벽 정책 개념 설명**
  * **Default Deny Incoming (기본 전면 차단)**: 외부에서 들어오는 모든 접속 시도를 일단 무조건 거부하는 안전 원칙 적용.
  * **최소 화이트리스트 포트 개방**: 업무에 꼭 필요한 2개 포트만 유일하게 개방.
    * `20022/tcp`: 원격 관리를 위한 커스텀 SSH 포트
    * `15034/tcp`: 애플리케이션 서비스를 위한 전용 포트

* **3.2 터미널 시연 진행**
  * **실행 명령어**:
    ```bash
    sudo ufw status verbose
    ```
  * **시연 설명 멘트**:
    * "UFW 방화벽 상태가 `active`로 활성화되어 있습니다."
    * "기본 정책은 `deny (incoming)`이며, 허용 목록에 `20022/tcp`와 `15034/tcp`만 등록되어 타격을 최소화했습니다."

---

## 📊 4. 검증 결과 정리 및 결론 (Conclusion & Q&A)

* **4.1 1-1 미션 달성 성과 요약**
  * SSH 포트 20022 전환 완료 (`PASS`)
  * Root 원격 접속 차단 완료 (`PASS`)
  * UFW 활성화 및 최소 포트 2개 허용 완료 (`PASS`)
  * 자동화 테스트 수트(`run_tests.sh`) 100% 통과 완료 (`PASS`)

* **4.2 예상 질문 및 답안 (Q&A 대응)**
  * **Q1. 왜 UFW에서 22번 포트를 열어두지 않나요?**
    * 💡 *A. SSH 포트를 20022로 변경했기 때문에 기존 22번 포트는 닫아두는 것이 보안상 안전하며, 허용 목록에 넣지 않는 것이 화이트리스트 원칙에 부합합니다.*
  * **Q2. Root 접속이 안 되면 시스템 관리는 어떻게 하나요?**
    * 💡 *A. 일반 계정(`agent-admin`)으로 먼저 접속한 뒤, 권한이 필요한 작업 시에만 `sudo` 명령어를 사용하여 안전하게 관리합니다.*

* **4.3 발표 마칭 멘트**
  * "이상으로 1-1 기본 보안 및 네트워크 설정에 대한 시연 및 설명을 마치겠습니다. 감사합니다!"
